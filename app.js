const axios = require('axios')

/**
 * @param  {string} url
 * @returns {Promise}
 */
const getRequest = (url) => 1


/**
 * @param  {object} body
 * @param  {string} url
 * @returns {Promise}
 */
const postRequest = (body, url) => 2



module.exports = {
  getRequest,
  postRequest
};
